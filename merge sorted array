def merge_sorted_arrays(arr1, arr2):
    return sorted(arr1 + arr2)  # Concatenate and sort the combined array

# Example usage:
arr1 = [1, 3, 5]
arr2 = [2, 4, 6]
print(merge_sorted_arrays(arr1, arr2))  # Output: [1, 2, 3, 4, 5, 6]
